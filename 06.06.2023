1) git checkout -- file
git checkout -- file1 file2 file3

2) git restore -- file

3) git revert text_commit

4) git branch new_branch
git checkout new_branch

5) Это два разных способа слияние веток. 
В первом случае - это слияние с быстрым перемещением, то есть в истории коммитов
не создается новый коммит слияния, так как изменения из другой ветки 
просто добавляются в текущую ветку. 
Обычно используется для слияния небольших и временных веток, 
или веток с исправлениями ошибок.

Во втором случае Git создает новый коммит слияния, объединяющий изменения из обеих
веток. 

6) Когда одна и та же часть файла была изменена в обеих ветках, 
которые пытаемся слить. Например, если две ветки внесли изменения в одну
и ту же часть файла. Или в одной ветке удален файл, а в другой ветке
внесены изменения в этот же файл.

7) git branch - d new_branch
